---
import { ClientRouter } from 'astro:transitions';
import BaseHead, { type Props as HeadProps } from '#components/BaseMeta.astro';
import Header from '#components/Header.astro';
import Footer from '#components/Footer.astro';
import Nav from '#components/Nav.astro';

export type Props = HeadProps;

const head = Astro.props;
---

<!doctype html>
<html lang="en" class="wrap-break-words antialiased">
	<head>
		<BaseHead {...head} />
		<script>
			if (localStorage.theme === 'dark') {
				document.documentElement.classList.add('dark');
			}
		</script>
		<ClientRouter />
		<script is:inline src="/theme-toggle.js"></script>
	</head>
	<body class="bg-main text-main">
		<Header />
		<main class="mx-auto mt-28 w-full max-w-3xl grow px-4">
			<slot />
		</main>
		<Footer />
	</body>
</html>
